# Virtual environments
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# IDE / editor
.idea/
.vscode/
*.swp

# Claude Code
.claude/

# OS
.DS_Store

# pytest
.pytest_cache/
pytest-of-*/
tmp*.db

# Test/runtime residue — flow_doctor.db is the default sqlite store path and
# gets written by anything run from the repo root without an explicit store
# override (alpha-engine-config-I8299)
/flow_doctor.db
*.db

# Design docs (local only)
*-design-*.md
*-design.md

.gitleaksbaseline.json

# Local-only automation scripts (e.g. publish.sh) — not distributed with the package
scripts/

# Internal planning docs (not for public repo)
private/
.coverage
htmlcov/

# Agent operating instructions — local-only.
# nous-ergon-ops/policies/repository-tiering-policy.md §5.3: these describe how WE
# work, not how a contributor gets started. Contributor-facing guidance belongs in
# CONTRIBUTING.md. Public repos keep these out of git by default.
/AGENTS.md
/CLAUDE.md

# Gitleaks config — a LOCAL symlink to the shared config, never committed.
# It was tracked until 2026-08-07 as a symlink to an absolute laptop path, which
# dangles in every CI checkout: local and CI then scanned under different rules
# and the locally-generated baseline did not match what CI found. See
# alpha-engine-config-I6622.
.gitleaks.toml
